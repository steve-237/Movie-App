import { MinToDurationPipe } from './min-to-duration.pipe';

describe('MinToDurationPipe', () => {
  it('create an instance', () => {
    const pipe = new MinToDurationPipe();
    expect(pipe).toBeTruthy();
  });
});
